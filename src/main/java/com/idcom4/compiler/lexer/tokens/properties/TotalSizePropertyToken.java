package com.idcom4.compiler.lexer.tokens.properties;

public class TotalSizePropertyToken extends PropertyToken {
    public TotalSizePropertyToken(String slice, int indexEnd) {
        super(slice, indexEnd);
    }
}
