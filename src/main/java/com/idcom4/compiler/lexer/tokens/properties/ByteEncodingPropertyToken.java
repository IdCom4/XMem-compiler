package com.idcom4.compiler.lexer.tokens.properties;

public class ByteEncodingPropertyToken extends PropertyToken {
    public ByteEncodingPropertyToken(String slice, int indexEnd) {
        super(slice, indexEnd);
    }
}
